<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KotlinApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->

        <item name="colorPrimary">#00d1ff</item>
        <item name="colorPrimaryDark">#00d1ff</item>
        <item name="colorAccent">#00d1ff</item> -- change this one

        <!-- Customize your theme here. -->
    </style>

    <style name="LightTheme" parent="Theme.KotlinApp">
        <item name="textColor">#000000</item>
        <item name="bgColor">#ffffff</item>
        <item name="drawable">@drawable/ic_launcher_background</item>
    </style>

    <style name="DarkTheme" parent="Theme.KotlinApp">
        <item name="textColor">#ffffff</item>
        <item name="bgColor">#000000</item>
        <item name="drawable">@drawable/ic_launcher_foreground</item>
    </style>

</resources>